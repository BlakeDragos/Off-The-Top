<div class="container">

  <!-- Page Heading -->
  <h1 class="my-4">Posts
    <small>from hot page</small>
    <a class="btn btn-primary" id="load" href="/all">Load</a>
    <a class="btn btn-primary loadS" id="loadS" href="/saved">Load Saved Posts</a>
  </h1>
  <ul>
    {{#each posts}}
      {{> posts/post-block}}
    {{/each}}
    {{#each saved}}
      {{> posts/saved-block}}
    {{/each}}
  </ul>

</div>